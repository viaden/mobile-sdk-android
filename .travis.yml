language: android
jdk: oraclejdk7

android:
  components:
    - platform-tools
    - tools
  licenses:
    - 'android-sdk-license-.+'

script:
  - ./gradlew clean build

branches:
  except:
    - gh-pages

notifications:
  email: false

sudo: false

cache:
  directories:
    - $HOME/.gradle