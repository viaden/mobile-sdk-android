language: android
sudo: false
cache: false

android:
  components:
    - platform-tools
    - tools
  licenses:
    - 'android-sdk-license-.+'

script:
  - ./gradlew clean build

branches:
  except:
    - gh-pages

notifications:
  email: false
